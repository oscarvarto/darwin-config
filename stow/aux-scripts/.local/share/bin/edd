#!/usr/bin/env zsh

# Start the home-manager Emacs service (replaces manual daemon management)
# This prevents conflicts between named daemons and the service daemon

echo "🚀 Starting Emacs service (home-manager managed daemon)..."
exec emacs-service-toggle start
